# React CVAT AnnotationCanvas
# Author Franklin Kemta
# We use this libray to drag customized (svgs) on top of an image / video / stream
# build on top of react-native-svg, react-native-reanimated v2, 